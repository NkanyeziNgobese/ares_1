# Unity Bridge

Unity work happens in the Unity Editor, but the scripts and topic map live here
for version control. These files are stubs for the MQTT bridge and telemetry
schema alignment with the Python pipeline.
